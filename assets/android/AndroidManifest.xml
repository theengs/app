<?xml version="1.0"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:installLocation="auto"
          android:versionName="-- %%INSERT_VERSION_NAME%% --" android:versionCode="-- %%INSERT_VERSION_CODE%% --">

    <supports-screens android:anyDensity="true" android:largeScreens="true" android:normalScreens="true" android:smallScreens="true" />

    <!-- Request legacy Bluetooth permissions (up to Android 11 / SDK 30) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:minSdkVersion="29" />

    <!-- Request new Bluetooth permissions (Android 12+ / SDK 31) -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:minSdkVersion="31" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" android:minSdkVersion="31" />

    <!-- Service -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Held briefly when the AllowWhileIdle work alarm fires, so the CPU
         stays awake through the background scan window in deep doze. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Lets the background-work alarm fire at the user's exact "Update
         interval" through doze (setExactAndAllowWhileIdle). Auto-granted on
         API 31-32, user-grantable on 33+; scheduleWork() degrades to an inexact
         AllowWhileIdle alarm when it isn't granted. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Lets the user exempt Theengs from battery optimizations via a one-tap
         system dialog, so Doze / app-standby won't defer the background-work
         alarms. NOTE: Google Play treats this as a policy-sensitive permission
         and may require a Play Console declaration for the listing. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" android:minSdkVersion="29" />
    <!-- Foreground-service permissions so the BLE-scan service can keep scanning
         past the screen-off cliff. FOREGROUND_SERVICE is required since API 28;
         the typed FOREGROUND_SERVICE_CONNECTED_DEVICE is required since API 34
         for BLE / companion-device services. Without these, startForegroundService()
         throws SecurityException and the service never runs. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" android:minSdkVersion="34" />

    <!-- MQTT -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Others -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" android:minSdkVersion="33"/>

    <!-- Hardware permissions -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <!-- Application -->
    <application android:name="org.qtproject.qt.android.bindings.QtApplication"
                 android:hardwareAccelerated="true" android:allowNativeHeapPointerTagging="false"
                 android:theme="@style/AppTheme" android:roundIcon="@mipmap/ic_launcher_round" android:icon="@mipmap/ic_launcher"
                 android:label="Theengs BLE"
                 android:allowBackup="true"
                 android:fullBackupContent="@xml/backup_rules"
                 android:dataExtractionRules="@xml/data_extraction_rules"
                 android:enableOnBackInvokedCallback="false">

        <!-- Activity -->
        <activity android:name="org.qtproject.qt.android.bindings.QtActivity"
                  android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
                  android:screenOrientation="unspecified" android:windowSoftInputMode="adjustPan" android:launchMode="singleTask" android:exported="true"
                  android:label="Theengs">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data android:name="android.app.lib_name" android:value="-- %%INSERT_APP_LIB_NAME%% --" />
            <meta-data android:name="android.app.arguments" android:value="-- %%INSERT_APP_ARGUMENTS%% --" />
            <meta-data android:name="android.app.extract_android_style" android:value="minimal" />
            <meta-data android:name="android.app.splash_screen_drawable" android:resource="@drawable/splash" />
            <meta-data android:name="android.app.splash_screen_sticky" android:value="true" />
            <meta-data android:name="android.app.background_running" android:value="false" />
        </activity>

        <!-- Android service(s) -->
        <receiver android:name=".TheengsBootServiceBroadcastReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service android:process=":qt_service" android:name=".TheengsAndroidService"
                 android:foregroundServiceType="connectedDevice">
            <meta-data android:name="android.app.background_running" android:value="true" />
            <meta-data android:name="android.app.lib_name" android:value="-- %%INSERT_APP_LIB_NAME%% --" />
            <meta-data android:name="android.app.arguments" android:value="--service" />
        </service>

    </application>

</manifest>
